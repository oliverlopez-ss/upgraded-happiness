name: Fetch HubSpot Deals

on:
  schedule:
    - cron: '0 */2 * * *'  # Var 2:a timme
  workflow_dispatch:        # Manuell körning

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Find pipeline ID for "New Sales"
        env:
          HUBSPOT_TOKEN: ${{ secrets.HUBSPOT_TOKEN }}
        id: pipeline
        run: |
          if [ -z "$HUBSPOT_TOKEN" ]; then
            echo "::error::HUBSPOT_TOKEN secret is not set! Go to Settings → Secrets → Actions and add it."
            exit 1
          fi
          echo "Token starts with: ${HUBSPOT_TOKEN:0:8}..."
          PIPELINES=$(curl -s \
            -H "Authorization: Bearer $HUBSPOT_TOKEN" \
            "https://api.hubapi.com/crm/v3/pipelines/deals")
          echo "Available pipelines:"
          echo "$PIPELINES" | python3 -c "import json,sys; [print(f'  {p[\"label\"]} -> {p[\"pipelineId\"]}') for p in json.load(sys.stdin).get('results',[])]"
          PIPELINE_ID=$(echo "$PIPELINES" | python3 -c "
          import json,sys
          for p in json.load(sys.stdin).get('results',[]):
            if 'new sales' in p['label'].lower():
              print(p['pipelineId']); break
          ")
          if [ -z "$PIPELINE_ID" ]; then
            echo "::error::Pipeline 'New Sales' not found!"
            exit 1
          fi
          echo "Using pipeline: $PIPELINE_ID"
          echo "pipeline_id=$PIPELINE_ID" >> "$GITHUB_OUTPUT"

      - name: Fetch open deals from "New Sales" pipeline
        env:
          HUBSPOT_TOKEN: ${{ secrets.HUBSPOT_TOKEN }}
          PIPELINE_ID: ${{ steps.pipeline.outputs.pipeline_id }}
        run: |
          mkdir -p warroom/data
          HTTP_CODE=$(curl -s -w "%{http_code}" \
            -X POST \
            -H "Authorization: Bearer $HUBSPOT_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{
              \"filterGroups\": [{
                \"filters\": [
                  {\"propertyName\": \"pipeline\", \"operator\": \"EQ\", \"value\": \"$PIPELINE_ID\"},
                  {\"propertyName\": \"hs_is_closed\", \"operator\": \"EQ\", \"value\": \"false\"}
                ]
              }],
              \"properties\": [\"dealname\", \"amount\", \"closedate\", \"hs_deal_stage_probability\", \"hs_is_closed\", \"pipeline\"],
              \"limit\": 100
            }" \
            "https://api.hubapi.com/crm/v3/objects/deals/search" \
            -o warroom/data/deals.json)
          echo "HTTP status: $HTTP_CODE"
          if [ "$HTTP_CODE" != "200" ]; then
            echo "::error::HubSpot Search API returned HTTP $HTTP_CODE"
            cat warroom/data/deals.json
            exit 1
          fi
          COUNT=$(python3 -c "import json; print(json.load(open('warroom/data/deals.json')).get('total',0))")
          echo "Success! Found $COUNT open deals in New Sales pipeline."
          cat warroom/data/deals.json | head -c 500

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add warroom/data/deals.json
          git diff --staged --quiet && echo "No changes" && exit 0
          git commit -m "Update HubSpot deals data"
          git push
